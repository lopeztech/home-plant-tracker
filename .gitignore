node_modules/
dist/
coverage/
.DS_Store
*.local

# Terraform — state files contain sensitive data, providers are large binaries
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfstate*

# Terraform temporary files
terraform/tmp/

# Integration test sample images (user-provided, not committed)
api/plants/integration/images/*.jpg
api/plants/integration/images/*.jpeg
api/plants/integration/images/*.png
